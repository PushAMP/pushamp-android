How to use?


1) Create configuration file pushamp_config.json and put to "assets".
2) Add library to classpath
3) PushAmp.getContext(context).registerForRemoteNotifications();
...
n) Profit!


Config example:

{"pushamp":
    {
    "apiKey":"someApiKey",
    "senderId", "someSenderId"
    }
}

